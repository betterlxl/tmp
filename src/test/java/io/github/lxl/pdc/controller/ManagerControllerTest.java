package io.github.lxl.pdc.controller;

import io.github.lxl.pdc.entity.Manager;
import io.github.lxl.pdc.service.ManagerService;
import org.junit.Assert;
import org.junit.Test;
import org.springframework.beans.factory.annotation.Autowired;

import static org.junit.Assert.*;

public class ManagerControllerTest {
/*    @Autowired
    private ManagerService managerService;
    @Test
    public void testGetManager(){
        Manager manager = managerService.getManager(1);
        Assert.assertEquals(manager.getPhone(),"15837867682");
    }
    @Test
    public void testSaveManager(){
        Manager manager = new Manager();
        manager.setId(7);
        manager.setName("cll");
        manager.setPhone("121");
        managerService.saveManager(manager);

    }*/
}